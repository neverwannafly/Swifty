let editorConfig = {
    theme: "twilight",
    smartIndent: false,
    lineNumbers: true,
    autofocus: true,
};

let outputConfig = {
    theme : "twilight",
    readOnly: 'nocursor',
}

let treeConfig = {
    theme: "twilight",
    readOnly: "nocursor",
}